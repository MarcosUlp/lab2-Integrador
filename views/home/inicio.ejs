<%- include('../partials/header') %>
<%- include('../partials/navbar') %>
<link rel="stylesheet" href="/public/css/inicio.css">
<main class="inicio-layout">
  <!-- 🔍 Barra superior -->
  <header class="barra-superior">
    <input type="text" placeholder="Buscar...">
    <div class="iconos">
      <span>💬</span>
      <span>👤</span>
      <span>☰</span>
    </div>
  </header>

  <!-- 🖼️ Zona central -->
  <div class="zona-central">
    <!-- 📤 Subida de álbum -->
    <section class="subir-album">
      <h2>Subir Álbum</h2>
      <form action="/imagenes/subir" method="POST" enctype="multipart/form-data">
        <input type="text" name="titulo" placeholder="Nombre del álbum" required>
        <div class="dropzone">
          <p>⬇️ Arrastrar o seleccionar imágenes</p>
          <input type="file" name="imagenes" multiple required>
        </div>
        <button type="submit">➕ Subir</button>
      </form>
    </section>

    <!-- 📚 Feed de álbumes -->
    <section class="feed-albumes">
      <h3>Tus álbumes</h3>
      <div class="galeria">
        <% if (albumes.length === 0) { %>
          <p>No tenés álbumes aún.</p>
        <% } else { %>
          <% albumes.forEach(album => { %>
            <div class="album">
              <h4><%= album.titulo %></h4>
              <!-- Aquí podrías agregar imágenes miniatura si querés -->
              <p>ID: <%= album.albumes_id %></p>
            </div>
          <% }) %>
        <% } %>
      </div>
    </section>
  </div>
</main>

<%- include('../partials/footer') %>
