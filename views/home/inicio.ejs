
<main class="inicio-layout">
  <!-- 🔍 Barra superior -->
   
  <!-- 🖼️ Zona central -->
  <div class="zona-central">
    <!-- 📤 Subida de álbum -->
    

    <!-- 📚 Feed de álbumes -->
    <section class="feed-albumes">
      <h3>Este es el inicio, proximamente mostrar albumes de usuarios que sigo:</h3>
      <div class="galeria">
        <% if (albumes.length === 0) { %>
          <p>No tenés álbumes aún.</p>
        <% } else { %>
          <% albumes.forEach(album => { %>
            <div class="album">
              <h4><%= album.titulo %></h4>
              <!-- Aquí podrías agregar imágenes miniatura si querés -->
              <p>ID: <%= album.albumes_id %></p>
            </div>
          <% }) %>
        <% } %>
      </div>
    </section>
  </div>
</main>
